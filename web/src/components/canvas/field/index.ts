// Punto de entrada del dominio field (canvas).
// FieldCanvas.tsx importa desde aquí.
// No confundir con @/lib/field — ese es el dominio de física pura.

export * from './drawFieldLines';
export * from './drawSources';
